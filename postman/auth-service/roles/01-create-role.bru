meta {
  name: Create Role
  type: http
  seq: 1
}

post {
  url: {{api-gateway-AuthSERVICE}}/roles
  body: json
  auth: inherit
}

body:json {
  {
    "name": "MANAGER",
    "description": "Manager role with specific permissions",
    "permissions": ["CREATE_DATA", "UPDATE_DATA", "APPROVE_DATA"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

