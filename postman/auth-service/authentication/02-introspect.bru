meta {
  name: Introspect Token
  type: http
  seq: 2
}

post {
  url: {{api-gateway-AuthSERVICE}}/auth/introspect
  body: json
  auth: none
}

body:json {
  {
    "token": "{{token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

