meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{api-gateway-AuthSERVICE}}/auth/logout
  body: json
  auth: none
}

body:json {
  {
    "token": "{{token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

