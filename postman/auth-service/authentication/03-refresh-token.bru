meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{api-gateway-AuthSERVICE}}/auth/refresh-token
  body: json
  auth: none
}

body:json {
  {
    "token": "{{token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

